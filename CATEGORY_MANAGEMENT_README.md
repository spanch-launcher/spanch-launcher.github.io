# Управление категориями форума NLRP

## Обзор

Реализована полная система управления категориями форума с динамическим хранением в LocalStorage.

## Основные возможности

### Для администраторов и основателя:
- ✅ Создание новых категорий
- ✅ Редактирование существующих категорий
- ✅ Удаление категорий (кроме защищенной "Общие правила")
- ✅ Просмотр статистики по категориям (количество тем и сообщений)

### Для всех пользователей:
- ✅ Просмотр всех категорий
- ✅ Просмотр тем в категориях
- ✅ Автоматическая инициализация с категорией "Общие правила"

## Использование

### Доступ к управлению категориями

1. Войдите как администратор или основатель (логин: `admin`, пароль: `admin123`)
2. Перейдите в админ-панель: `admin.html`
3. В разделе "Управление категориями" доступны все функции

### Создание категории

1. Нажмите кнопку "+ Создать категорию"
2. Заполните поля:
   - **Название** - полное название с эмодзи (например: "💬 Общие обсуждения")
   - **Иконка** - эмодзи для отображения (например: "💬")
   - **Описание** - краткое описание категории
3. Нажмите "Создать"

### Редактирование категории

1. В таблице категорий нажмите "Редактировать" рядом с нужной категорией
2. Измените необходимые поля
3. Нажмите "Сохранить"

**Примечание:** Темы остаются связанными с категорией после редактирования.

### Удаление категории

1. В таблице категорий нажмите "Удалить" рядом с нужной категорией
2. Подтвердите удаление

**Важно:** 
- Категория "Общие правила" защищена от удаления
- При удалении категории удаляются все темы в ней

## Технические детали

### Файлы

- `categoryStorage.js` - модуль управления категориями
- `forum.js` - обновлен для динамической загрузки категорий
- `admin.js` - функции управления в админ-панели
- `categoryStorage.test.js` - property-based тесты (100 итераций каждый)
- `test-runner.html` - запуск тестов в браузере

### Структура данных

```javascript
{
  id: 1704067200000,              // Уникальный ID (timestamp)
  key: "general-rules",           // Ключ для ссылок
  name: "📋 Общие правила",       // Отображаемое название
  icon: "📋",                     // Эмодзи иконка
  description: "Описание...",     // Описание категории
  createdAt: "2026-01-10 12:00",  // Дата создания
  protected: true                 // Защита от удаления
}
```

### LocalStorage ключи

- `forumCategories` - массив категорий
- `forumTopics` - массив тем (существующий)
- `forumUsers` - массив пользователей (существующий)

## Тестирование

### Запуск тестов в браузере

1. Откройте `test-runner.html` в браузере
2. Нажмите "Run Tests"
3. Просмотрите результаты

### Запуск тестов в Node.js

```bash
node run-tests.js
```

### Покрытие тестами

- ✅ 14 property-based тестов (по 100 итераций)
- ✅ 4 unit теста
- ✅ Все 14 correctness properties из design.md

## Миграция

При первой загрузке после обновления:
1. Старые жестко закодированные категории удалены
2. Создается только категория "Общие правила"
3. Существующие темы остаются без изменений

## Контроль доступа

| Роль | Просмотр | Создание | Редактирование | Удаление |
|------|----------|----------|----------------|----------|
| Игрок | ✅ | ❌ | ❌ | ❌ |
| Админ | ✅ | ✅ | ✅ | ✅ |
| Основатель | ✅ | ✅ | ✅ | ✅ |

## Валидация

Все поля обязательны:
- Название не может быть пустым
- Иконка не может быть пустой
- Описание не может быть пустым

Пустые строки и строки из пробелов отклоняются.
